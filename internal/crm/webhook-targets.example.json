{
  "id": "webhook-targets-example",
  "version": "1.0.0",
  "visibility": "server_side_internal_example",
  "targets": {
    "genericWebhook": {
      "enabled": false,
      "envVar": "SCORECARD_LEAD_WEBHOOK_URL",
      "description": "POST normalized scorecard completion payload to Make, Zapier, Pipedream, n8n, or a custom HTTPS endpoint."
    },
    "n8n": {
      "enabled": false,
      "envVar": "N8N_SCORECARD_WEBHOOK_URL",
      "description": "Deprecated backward-compatible fallback for the generic webhook destination."
    },
    "hubspot": {
      "enabled": false,
      "envVar": "HUBSPOT_PRIVATE_APP_TOKEN",
      "description": "Future direct HubSpot adapter using the internal field map."
    },
    "notion": {
      "enabled": false,
      "envVar": "NOTION_TOKEN",
      "description": "Create a Funding Pipeline database record."
    },
    "airtable": {
      "enabled": false,
      "envVar": "AIRTABLE_API_KEY",
      "description": "Create a funding lead row."
    },
    "googleSheets": {
      "enabled": false,
      "envVar": "GOOGLE_SERVICE_ACCOUNT_JSON",
      "description": "Append score result to a sheet."
    }
  }
}
